// Image assets index
// This file will be used to export all image assets

export const images = {
  // Logo assets
  logo: require('./logo.png'),

  // Placeholder for other images
  // placeholder: require('./placeholder.png'),

  // Icons (if we add custom icons)
  // qrCode: require('./icons/qr-code.png'),
  // points: require('./icons/points.png'),
};

// SVG assets (if using react-native-svg)
export const svgAssets = {
  // Will be populated with SVG components
};
